import { FormPageWrapper } from "@/components/FormPageWrapper";
import { RegisterContainer } from "@/features/User/Register";

const RegisterPage = () => {
  return (
    <FormPageWrapper>
      <RegisterContainer />
    </FormPageWrapper>
  );
};

export default RegisterPage;
