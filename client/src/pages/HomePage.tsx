import { Box } from "@mui/material";

const HomePage = () => {
  return (
    <Box
      display="flex"
      height="100%"
      width="100%"
      justifyContent="center"
      alignItems="center"
    >
      This is a home page. You should see it only if you have authorized
    </Box>
  );
};

export default HomePage;
